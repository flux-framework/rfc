# do not check data files, assumed special / unique
# do not check spell checker file
[files]
extend-exclude = [
  "data/*",
  "spell.en.pws",
]

[default.extend-words]
# HPE = Hewlett Packard Enterprise (hardware vendor)
HPE = "HPE"
